name: mvs_reader
description: "HTML-читалка для працівника"
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.1
  shared_preferences: ^2.2.2
  html: ^0.15.6
  path_provider: ^2.1.2
  path: ^1.9.0
  highlight_text: ^1.2.0
  uuid: ^4.5.1
  flutter_widget_from_html: ^0.17.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/TEST.html
    - assets/TEST_Filter.html
    - assets/Original_Dovidnik HTML Filter.files/
    - assets/chapters.json
